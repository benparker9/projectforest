window.addEventListener('load', function() {
    const container = document.getElementById('container');
    container.style.transition = 'left 1s ease-in-out'; // Add smooth transition
    container.style.left = '0'; // Slide the content into vi
})